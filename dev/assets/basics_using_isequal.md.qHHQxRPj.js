import{_ as a,c as s,a4 as o,o as n}from"./chunks/framework.CEeYBNx8.js";const u=JSON.parse('{"title":"Comparing GeoRegions","description":"","frontmatter":{},"headers":[],"relativePath":"basics/using/isequal.md","filePath":"basics/using/isequal.md","lastUpdated":null}'),i={name:"basics/using/isequal.md"};function t(l,e,p,c,d,r){return n(),s("div",null,e[0]||(e[0]=[o(`<h1 id="Comparing-GeoRegions" tabindex="-1">Comparing GeoRegions <a class="header-anchor" href="#Comparing-GeoRegions" aria-label="Permalink to &quot;Comparing GeoRegions {#Comparing-GeoRegions}&quot;">​</a></h1><p>Now, suppose we have two different GeoRegions, we have constructed several different ways of testing various forms of equivalence.</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>using GeoRegions</span></span>
<span class="line"><span>using DelimitedFiles</span></span>
<span class="line"><span>using CairoMakie</span></span>
<span class="line"><span></span></span>
<span class="line"><span>download(&quot;https://raw.githubusercontent.com/natgeo-wong/GeoPlottingData/main/coastline_resl.txt&quot;,&quot;coast.cst&quot;)</span></span>
<span class="line"><span>coast = readdlm(&quot;coast.cst&quot;,comments=true)</span></span>
<span class="line"><span>clon  = coast[:,1]</span></span>
<span class="line"><span>clat  = coast[:,2]</span></span>
<span class="line"><span>nothing</span></span></code></pre></div><h2 id="1.-Are-two-GeoRegions-Equivalent" tabindex="-1">1. Are two GeoRegions Equivalent <a class="header-anchor" href="#1.-Are-two-GeoRegions-Equivalent" aria-label="Permalink to &quot;1. Are two GeoRegions Equivalent {#1.-Are-two-GeoRegions-Equivalent}&quot;">​</a></h2><p>If you want to check that two GeoRegions are equivalent, we can use the <code>==</code> or <code>isequal()</code> functions.</p><h3 id="1.-Strict-Equivalence" tabindex="-1">1. Strict Equivalence <a class="header-anchor" href="#1.-Strict-Equivalence" aria-label="Permalink to &quot;1. Strict Equivalence {#1.-Strict-Equivalence}&quot;">​</a></h3><p>For <code>==</code> or <code>isequal(geo1,geo2,strict = true)</code> to return true, the following conditions must be satisfied:</p><ul><li><p>The <code>ID</code> and <code>pID</code> fields are the same</p></li><li><p>The <code>shape</code> fields define exactly the same geographic region as per the function <a href="/GeoRegions.jl/dev/basics/using/ison"><code>on()</code></a></p></li><li><p>The <code>GeoRegion</code> must be of the same <code>type</code> (i.e., a <code>RectRegion</code> will not considered to be equivalent to a <code>PolyRegion</code> even if they have the same shape)</p></li></ul><p>Let us consider the following example. First, we load a <code>RectRegion</code> and extract its <code>lon</code> and <code>lat</code> coordinates</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">geo </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> GeoRegion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;GF_SEA&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">lon,lat </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> coordinates</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(geo)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">nothing</span></span></code></pre></div><p>We define <code>RectRegion</code>s that are of the same shape (note that because <code>save = false</code> that the same <code>ID</code> can be used to define these GeoRegions):</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>geo2 = RectRegion(&quot;GF_SEA&quot;,&quot;GLB&quot;,&quot;&quot;,geo.bounds) # Different name</span></span>
<span class="line"><span>geo3 = RectRegion(&quot;SEA&quot;,&quot;GLB&quot;,geo.name,geo.bounds) # Different ID</span></span>
<span class="line"><span>geo4 = PolyRegion(&quot;GF_SEA&quot;,&quot;GLB&quot;,geo.name,lon,lat) # Different ID</span></span>
<span class="line"><span>geo5 = PolyRegion(&quot;GF_SEA&quot;,&quot;GLB&quot;,&quot;&quot;,lon,lat) # Different ID</span></span>
<span class="line"><span>geo6 = PolyRegion(&quot;GF_SEA&quot;,&quot;GLB&quot;,geo.name,lon.+360,lat) # Shifted by 360º</span></span></code></pre></div><p>And we perform the tests with <code>==</code>:</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>geo == geo2, # true,  name is not a factor in considering equivalence</span></span>
<span class="line"><span>geo == geo3, # false, different ID</span></span>
<span class="line"><span>geo == geo4  # false, different GeoRegion type</span></span></code></pre></div><p>See the API nfor <code>==</code> <a href="/GeoRegions.jl/dev/api/isinonequal#Base.==">here</a></p><p>And we perform the tests with <code>isequal()</code></p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>isequal(geo,geo2), # true,  name is not a factor in considering equivalence</span></span>
<span class="line"><span>isequal(geo,geo3), # false, different ID</span></span>
<span class="line"><span>isequal(geo,geo4)  # false, different GeoRegion type</span></span></code></pre></div><p>See the API nfor <code>isequal()</code> <a href="/GeoRegions.jl/dev/api/isinonequal#Base.isequal">here</a></p><h3 id="1.2-Non-Strict-Equivalence" tabindex="-1">1.2 Non-Strict Equivalence <a class="header-anchor" href="#1.2-Non-Strict-Equivalence" aria-label="Permalink to &quot;1.2 Non-Strict Equivalence {#1.2-Non-Strict-Equivalence}&quot;">​</a></h3><p><code>isequal()</code> also has a <code>strict = false</code> option, where we do not care if the <code>GeoRegion</code>s being compared are of the same <code>type</code> (i.e., a <code>RectRegion</code> can be equivalent to a <code>PolyRegion</code> as long as the <code>ID</code>, <code>pID</code> and <code>shape</code> fields are equivalent).</p><p>So, we can see that</p><div class="language-@example vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">@example</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>isequal(geo,geo4,strict=false), # true, the shape is the same even though the GeoRegions are of differen types</span></span>
<span class="line"><span>isequal(geo,geo5,strict=false), # false, the ID is different</span></span>
<span class="line"><span>isequal(geo,geo6,strict=false), # true, shape is same even tho shift is by 360º</span></span></code></pre></div><h2 id="2.-Does-there-already-exist-a-GeoRegion?" tabindex="-1">2. Does there already exist a GeoRegion? <a class="header-anchor" href="#2.-Does-there-already-exist-a-GeoRegion?" aria-label="Permalink to &quot;2. Does there already exist a GeoRegion? {#2.-Does-there-already-exist-a-GeoRegion?}&quot;">​</a></h2><p>Sometimes, we want to figure out if there exists a <code>GeoRegion</code> <code>geo</code> in a project we have defined (for more information on how to use GeoRegions.jl in projects, see <a href="/GeoRegions.jl/dev/tutorials/projects">here</a>). We can use the function <code>isgeo()</code> to determine if this is the case.</p>`,24)]))}const g=a(i,[["render",t]]);export{u as __pageData,g as default};
