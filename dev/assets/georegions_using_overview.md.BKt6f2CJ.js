import{_ as i,c as s,a5 as o,o as t}from"./chunks/framework.D0cFjeDR.js";const r=JSON.parse('{"title":"Using GeoRegions.jl to specify custom GeoRegions","description":"","frontmatter":{},"headers":[],"relativePath":"georegions/using/overview.md","filePath":"georegions/using/overview.md","lastUpdated":null}'),a={name:"georegions/using/overview.md"};function n(l,e,d,p,c,h){return t(),s("div",null,e[0]||(e[0]=[o(`<h1 id="Using-GeoRegions.jl-to-specify-custom-GeoRegions" tabindex="-1">Using GeoRegions.jl to specify custom GeoRegions <a class="header-anchor" href="#Using-GeoRegions.jl-to-specify-custom-GeoRegions" aria-label="Permalink to &quot;Using GeoRegions.jl to specify custom GeoRegions {#Using-GeoRegions.jl-to-specify-custom-GeoRegions}&quot;">​</a></h1><p>Recall that there are three different types of <code>GeoRegion</code>s: (a) <code>RectRegion</code>s, (b) <code>TiltRegion</code>s and (c) <code>PolyRegion</code>s.</p><h2 id="Creating-new-GeoRegions" tabindex="-1">Creating new GeoRegions <a class="header-anchor" href="#Creating-new-GeoRegions" aria-label="Permalink to &quot;Creating new GeoRegions {#Creating-new-GeoRegions}&quot;">​</a></h2><p>We use the functions <code>RectRegion()</code>, <code>TiltRegion()</code> and <code>PolyRegion</code> to create new <code>GeoRegion</code>s of their respective types. Regardless of <code>GeoRegion</code> type, the <strong>first three inputs are always the same</strong>, in respective order:</p><ol><li><p>The ID (<code>ID</code>)</p></li><li><p>parent <code>GeoRegion</code> ID, (<code>pID</code>)</p></li><li><p>Name of the GeoRegion (<code>name</code>)</p></li></ol><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">RectRegion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ID, pID, name, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">TiltRegion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ID, pID, name, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PolyRegion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ID, pID, name, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">Constraints on <code>ID</code> and <code>pID</code> when <code>save = true</code></p><p>When <code>save = true</code>, the GeoRegion <code>pID</code> must already have been previously defined, and the region defined by the GeoRegion <code>ID</code> must be entirely within the region defined by the GeoRegion <code>pID</code>.</p></div><h2 id="Saving-new-GeoRegions" tabindex="-1">Saving new GeoRegions <a class="header-anchor" href="#Saving-new-GeoRegions" aria-label="Permalink to &quot;Saving new GeoRegions {#Saving-new-GeoRegions}&quot;">​</a></h2><p>As of <code>≥v7</code> and above, new GeoRegions are no longer automatically saved. If you wish to automatically save a new GeoRegions <strong>as it is created</strong>, specify the keyword argument <code>save = true</code>. To specify the directory to which the GeoRegion information is saved to, use the <code>path</code> keyword.</p><ul><li><p><code>RectRegion(ID, pID, name, ..., save = true, path = ...)</code> writes to <code>$path/rectlist.txt</code></p></li><li><p><code>TiltRegion(ID, pID, name, ..., save = true, path = ...)</code> writes to <code>$path/tiltlist.txt</code></p></li><li><p><code>PolyRegion(ID, pID, name, ..., save = true, path = ...)</code> writes to <code>$path/polylist.txt</code></p></li></ul><div class="tip custom-block"><p class="custom-block-title">Default <code>path</code> Directory</p><p>By default, <code>path = joinpath(DEPOT_PATH[1],&quot;files&quot;,&quot;GeoRegions&quot;)</code>. If <code>path</code> is not specified, the information will be saved in the respective custom lists in this directory.</p></div><div class="warning custom-block"><p class="custom-block-title">Modification of Custom Lists</p><p>While it is possible to do manually modify the lists, it is not recommended to do so, especially for <code>polylist.txt</code>, which is pretty complicated. Instead, you should let GeoRegions.jl do most of the heavy lifting.</p></div><p>You can also add a <code>GeoRegion</code> variable in the workspace that you have not yet saved into the custom lists</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">geo </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> PolyRegion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ID, pID, name, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(geo, path </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&lt;directory&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h2 id="Calling-saved-GeoRegions" tabindex="-1">Calling saved GeoRegions <a class="header-anchor" href="#Calling-saved-GeoRegions" aria-label="Permalink to &quot;Calling saved GeoRegions {#Calling-saved-GeoRegions}&quot;">​</a></h2><p>If a GeoRegion has been saved to a <code>path</code>, it and its properties can be called using its <code>ID</code> using the function <code>GeoRegion()</code>. There is no need to specify the GeoRegion subtype, as <code>GeoRegion</code>s with the same <code>ID</code> <strong>cannot</strong> be saved to the same <code>path</code>, even if they are of different subtypes.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">geo </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> GeoRegion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ID, path </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&lt;directory&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div>`,17)]))}const k=i(a,[["render",n]]);export{r as __pageData,k as default};
